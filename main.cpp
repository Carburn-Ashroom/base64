/**
 * Base64加解密工具
 * 用于加解密Base64编码
 * C++（Qt）编写，MinGW-w64编译
 * 作者：Carburn Ashroom
 */

#include "mainwindow.h"

#include <QApplication>

int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    MainWindow w;
    w.show();
    return a.exec();
}
